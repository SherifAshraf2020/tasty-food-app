package com.example.tasty_food_app;

//Talk with model and present that data to view
//Determine the state of view show error show data  (just Determine not Implement)
public interface SplashPresenter {
    void checkDestination();
}
